<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- LAN discovery + transport -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Wi-Fi Aware (Phase 5 step 3); hardware-gated, checked at runtime -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" tools:targetApi="s" />
    <uses-feature android:name="android.hardware.wifi.aware" android:required="false" />
    <!-- Bluetooth HID peripheral mode (the headline feature) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <!-- Android TV / Google TV viewer (spec §9 Phase 6 step 2): the same client
         runs on TV. Touchscreen not required; leanback (D-pad) launcher below. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <!-- Foreground service that runs the node + screen capture -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="@string/app_name"
        android:icon="@android:drawable/sym_def_app_icon"
        android:banner="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Conduit">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|density|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Also appear on the Android TV / Google TV home screen. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The node runs in a foreground service so transfers/streams survive
             the app going to the background (spec §9 Phase 5).

             connectedDevice ONLY. This service starts at launch, and from
             Android 14 a service may not claim the mediaProjection type before
             the user has granted a projection — declaring it here would make
             every launch a SecurityException on a modern phone. Screen capture
             therefore lives in its own service below, started after consent. -->
        <service
            android:name=".ConduitService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Screen capture (AND-2). Started only once MediaProjection consent
             has been granted, which is what makes claiming this type legal. -->
        <service
            android:name=".ScreenShareService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Input RECEIVER: Android can be controlled by a peer via
             AccessibilityService#dispatchGesture. Gated OFF by default; the user
             enables it in system settings (Play review scrutiny — spec pitfall). -->
        <service
            android:name=".capability.InputAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Notification SOURCE: mirror this device's notifications to peers via
             NotificationListenerService, with per-app filtering. User-enabled. -->
        <service
            android:name=".capability.NotificationSourceService"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
